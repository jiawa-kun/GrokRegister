name: Docker Turnstile Solver multi-arch

# Avoid docker/metadata-action (can fail with GitHub Unicorn HTML / API 5xx).
# Tags are set explicitly; image name is fixed lowercase for GHCR.

on:
  push:
    branches: [beta_dev]
    tags: ["v*"]
    paths:
      - "docker/turnstile-solver/**"
      - ".github/workflows/docker-turnstile-solver.yml"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  # must match root docker-compose.yml (all lowercase)
  IMAGE: ghcr.io/garrenkun/grok-turnstile-solver

jobs:
  build-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker tags
        id: tags
        run: |
          set -euo pipefail
          SHA_SHORT="$(echo '${{ github.sha }}' | cut -c1-7)"
          TAGS="${{ env.IMAGE }}:sha-${SHA_SHORT}"
          if [ "${{ github.ref }}" = "refs/heads/beta_dev" ]; then
            TAGS="${TAGS},${{ env.IMAGE }}:beta_dev,${{ env.IMAGE }}:beta-dev,${{ env.IMAGE }}:latest"
          elif [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            VER="${{ github.ref_name }}"
            TAGS="${TAGS},${{ env.IMAGE }}:${VER}"
            # v1.2.3 -> also 1.2
            MINOR="$(echo "$VER" | sed -E 's/^v([0-9]+\.[0-9]+).*/\1/')"
            if [ -n "$MINOR" ] && [ "$MINOR" != "$VER" ]; then
              TAGS="${TAGS},${{ env.IMAGE }}:${MINOR}"
            fi
          else
            BRANCH="$(echo '${{ github.ref_name }}' | tr '[:upper:]' '[:lower:]' | tr -c 'a-z0-9._-' '-')"
            TAGS="${TAGS},${{ env.IMAGE }}:${BRANCH}"
          fi
          echo "tags=${TAGS}" >> "$GITHUB_OUTPUT"
          echo "tags=${TAGS}"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push (linux/amd64 + linux/arm64)
        uses: docker/build-push-action@v6
        with:
          context: ./docker/turnstile-solver
          file: ./docker/turnstile-solver/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          cache-from: type=gha,scope=turnstile-solver
          cache-to: type=gha,mode=max,scope=turnstile-solver
